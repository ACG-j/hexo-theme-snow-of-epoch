<div id="banner">
  <div class="banner-image">
    <img src="{{ theme.banner.image }}" alt="{{ theme.banner.title }}">
    <div class="ink-splash">
      <div class="title-container">
        <h1 class="banner-title">{{ theme.banner.title }}</h1>
        <p class="banner-subtitle">{{ theme.banner.subtitle }}</p>
      </div>
    </div>
  </div>
</div>